{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.4"
  },
  "scripts": {
    "start-dev": "nodemon index.js",
    "start-prod": "node index.js",
    "get-ui": "rm -r build && cd ../react-ui && yarn build && mv build ../server",
    "start-update-ui-dev": "yarn get-ui && yarn start-dev"
  }
}
